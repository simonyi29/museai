# MuseAI

MuseAI is a local Obsidian plugin package based on Claudian, renamed and configured as a separate plugin.

## Included Files

- `main.js`
- `manifest.json`
- `styles.css`

Local runtime settings such as `data.json` and backup files are intentionally ignored.
